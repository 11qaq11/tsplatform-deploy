# 中科影像开发平台（TsPlatform）— 项目交接文档

**日期**: 2026-07-26  
**服务器**: 122.51.90.193  
**代码仓库**: https://github.com/11qaq11/tsplatform-deploy

---

## 一、项目概述

基于 GitHub 开源项目 [BitFun](https://github.com/GCWing/BitFun) 二次开发的影像开发桌面智能 Agent 平台。

| 项目 | 说明 |
|------|------|
| 项目名称 | 中科影像开发平台 / TsPlatform |
| 技术栈 | Tauri v2 + Rust + TypeScript（桌面端）、Node.js（云端） |
| 部署方式 | Docker Compose 单机部署 |
| 登录方式 | 飞书企业自建应用 OAuth |

---

## 二、已完成工作

### 2.1 规范文档（`specs/001-imaging-dev-platform/`）

| 文档 | 内容 |
|------|------|
| `spec.md` | 功能规格说明书（37 条功能需求、11 条成功标准、5 个用户故事） |
| `plan.md` | 实施计划（技术选型、项目结构、8 个实施阶段） |
| `tasks.md` | 88 条开发任务（含已完成/待完成标记） |
| `research.md` | 8 项技术决策记录 |
| `data-model.md` | 7 个实体定义 + 状态生命周期 |
| `contracts/api.md` | REST API 契约（6 组接口） |
| `quickstart.md` | 6 个端到端验证场景 |

### 2.2 已实施代码

| 层级 | 内容 | 状态 |
|------|------|------|
| 品牌替换 | `package.json`、`Cargo.toml`、`tauri.conf.json` 关键配置已替换 | 部分完成 |
| 批量替换脚本 | `scripts/brand-replace.ps1` — 在开发机上执行 | 待执行 |
| 云端服务（TypeScript 版） | Hono 框架，含 auth/sync/tools/admin 路由 | 已编写 |
| 云端服务（部署版） | 纯 Node.js，零框架依赖，已推送到 GitHub | ✅ 完成 |
| Docker 部署 | Dockerfile + docker-compose.yml + nginx.conf | ✅ 完成 |

### 2.3 部署包（GitHub）

仓库: **https://github.com/11qaq11/tsplatform-deploy**

```
tsplatform-deploy/
├── Dockerfile              # Node.js 20 Alpine 镜像
├── docker-compose.yml      # 单服务编排（端口 3000）
├── .env.example            # 飞书凭证模板
├── README.md               # 部署说明
├── .gitignore
└── packages/cloud-server/
    ├── package.json
    └── src/
        └── index.js         # 完整 API 服务（290行）
```

---

## 三、服务器部署

### 3.1 克隆部署包

```bash
ssh ubuntu@122.51.90.193
cd ~/workspace
git clone https://github.com/11qaq11/tsplatform-deploy.git
cd tsplatform-deploy
```

### 3.2 配置飞书凭证

```bash
cp .env.example .env
vim .env
```

填入实际的飞书企业自建应用凭证：

```env
FEISHU_APP_ID=cli_xxxxxxxxxxxx
FEISHU_APP_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx
ADMIN_FEISHU_OPEN_ID=ou_xxxxxxxxxxxx
```

### 3.3 启动服务

```bash
docker compose up -d --build
```

### 3.4 验证

```bash
curl http://localhost:3000/api/v1/health
# 预期: {"status":"ok","timestamp":"2026-..."}
```

### 3.5 防火墙放行

```bash
sudo ufw allow 3000/tcp
sudo ufw allow 8080/tcp
```

### 3.6 飞书开放平台配置

登录 [飞书开放平台](https://open.feishu.cn)，进入你的企业自建应用：

- 安全设置 → **重定向 URL**: `http://122.51.90.193:3000/api/v1/auth/feishu/callback`
- 确保应用已发布且对组织内成员可见

---

## 四、服务端口

| 服务 | 端口 | 地址 |
|------|------|------|
| Cloud REST API | 3000 | `http://122.51.90.193:3000/api/v1/health` |
| 管理后台（规划中） | 8080 | `http://122.51.90.193:8080` |

---

## 五、待完成工作

| 阶段 | 内容 | 优先级 |
|------|------|--------|
| Phase 2（T009-T021） | 执行 `scripts/brand-replace.ps1` 批量品牌替换 | P0 |
| Phase 2（T019-T020） | 验证 `cargo build` + `pnpm build` 通过 | P0 |
| Phase 4（US1） | 桌面客户端飞书登录 UI + 双模式存储 + 同步 | P1 |
| Phase 5（US2） | 工具市场（浏览/下载/点赞/管理） | P2 |
| Phase 6（US3） | 个人空间（对话/操作/错误/文件） | P3 |
| Phase 7（US5） | 后台操作历史 + 管理面板 | P5 |
| Phase 8 | 安装包构建 + 自动更新 | Polish |

---

## 六、关键环境变量

| 变量 | 说明 | 位置 |
|------|------|------|
| `FEISHU_APP_ID` | 飞书应用 ID | `.env` / `docker-compose.yml` |
| `FEISHU_APP_SECRET` | 飞书应用密钥（仅服务端） | `.env` / `docker-compose.yml` |
| `ADMIN_FEISHU_OPEN_ID` | 管理员飞书 OpenID | `.env` / `docker-compose.yml` |
| `API_BASE_URL` | 对外 API 地址 | docker-compose 固定 |
| `DB_PATH` | SQLite 数据库路径 | `/data/tsplatform.db` |

---

## 七、常用运维命令

```bash
# 查看日志
docker compose logs -f

# 重启服务
docker compose restart

# 停止服务
docker compose down

# 重新构建并启动
docker compose up -d --build

# 进入容器
docker compose exec cloud-server sh

# 查看数据库
docker compose exec cloud-server sqlite3 /data/tsplatform.db ".tables"

# 设置用户为管理员
docker compose exec cloud-server sqlite3 /data/tsplatform.db "UPDATE users SET role='admin' WHERE username='用户名';"
```

---

## 八、文件清单

### 规范文档（本机 `E:\workspace\TsPlatform\specs\`）

| 文件 | 大小 |
|------|------|
| `spec.md` | 234 行 |
| `plan.md` | 95 行 |
| `tasks.md` | 295 行（88 任务） |
| `research.md` | 80 行 |
| `data-model.md` | 157 行 |
| `contracts/api.md` | 120 行 |
| `quickstart.md` | 185 行 |
| `checklists/requirements.md` | 35 行 |

### 源代码（本机 `E:\workspace\TsPlatform\`）

| 目录 | 内容 | 状态 |
|------|------|------|
| `src/` | BitFun 原始代码（5000+ 文件） | 克隆完成，品牌替换部分执行 |
| `packages/cloud-server/src/` | 云端服务（14 个 TS 源文件） | 已编写 |
| `packages/admin-panel/` | 管理后台骨架 | 仅目录 + package.json |
| `docker/` | Docker 配置（5 个文件） | 已编写 |
| `platform_tools/` | 工具市场存储 | 仅 manifest-schema.json |
| `scripts/brand-replace.ps1` | 批量品牌替换脚本 | 待执行 |
| `tsplatform.config.example.json` | 桌面端配置模板 | 已创建 |
